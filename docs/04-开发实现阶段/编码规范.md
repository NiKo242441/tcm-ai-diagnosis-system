# 中医数字化诊疗平台编码规范

## 1. 规范概述

### 1.1 规范目标
本编码规范旨在为中医数字化诊疗平台建立统一的代码风格和质量标准，确保代码的可读性、可维护性和可扩展性，促进团队协作，提高开发效率。

### 1.2 适用范围
- **后端开发**：.NET 8.0 C# 代码
- **前端开发**：Vue.js 3.0 + TypeScript
- **数据库开发**：MySQL 8.0 SQL脚本
- **AI服务**：Python 3.11
- **脚本工具**：Shell、PowerShell

### 1.3 代码质量目标
```yaml
质量标准:
  代码覆盖率: > 80%
  复杂度: Cyclomatic Complexity < 10
  重复代码: < 3%
  技术债务: 0个Blocker, 0个Critical
  性能要求: API响应时间 < 500ms
```

## 2. 命名规范

### 2.1 C#命名规范

#### 2.1.1 基本命名规则
```csharp
// 类名 - 使用PascalCase
public class PatientService
public class DiagnosisResult

// 接口名 - 使用PascalCase，前缀为I
public interface IPatientRepository
public interface IDiagnosisEngine

// 方法名 - 使用PascalCase
public async Task<DiagnosisResult> AnalyzeSymptomsAsync(SymptomInput input)
public Patient GetPatientById(int patientId)

// 属性名 - 使用PascalCase
public string PatientName { get; set; }
public DateTime DiagnosisDate { get; private set; }

// 字段名 - 使用camelCase，私有字段前缀_
private readonly IPatientRepository _patientRepository;
private readonly ILogger<PatientService> _logger;

// 常量名 - 使用PascalCase
public const int MaxPrescriptionItems = 10;
public const string DefaultConnectionStringName = "DefaultConnection";

// 枚举名 - 使用PascalCase，枚举值使用PascalCase
public enum DiagnosisStatus
{
    Pending,
    InProgress,
    Completed,
    Failed
}

// 局部变量 - 使用camelCase
var patient = await _patientRepository.GetByIdAsync(patientId);
var diagnosisResults = new List<DiagnosisResult>();
```

#### 2.1.2 命名约定表
```yaml
类型              命名风格      示例
类名              PascalCase   PatientService
接口名            PascalCase   IPatientService
方法名            PascalCase   GetPatientById
属性名            PascalCase   PatientName
字段名            camelCase    _patientRepository
常量名            PascalCase   MaxRetryCount
枚举名            PascalCase   DiagnosisStatus
局部变量          camelCase    patientList
参数名            camelCase    patientId
命名空间          PascalCase   TcmAiDiagnosis.Services
```

### 2.2 TypeScript命名规范

#### 2.2.1 前端命名规则
```typescript
// 组件名 - 使用PascalCase
export default class PatientCardComponent extends Vue {}
export default class DiagnosisFormComponent extends Vue {}

// 接口名 - 使用PascalCase
interface Patient {
  id: number;
  name: string;
  age: number;
}

interface DiagnosisRequest {
  symptoms: Symptom[];
  patientId: number;
}

// 类型名 - 使用PascalCase
type DiagnosisStatus = 'pending' | 'processing' | 'completed' | 'failed';
type UserRole = 'patient' | 'doctor' | 'admin';

// 函数名 - 使用camelCase
function calculateDiagnosisScore(symptoms: Symptom[]): number {}
async function fetchPatientData(patientId: number): Promise<Patient> {}

// 变量名 - 使用camelCase
const patientList: Patient[] = [];
let currentDiagnosis: Diagnosis | null = null;

// 常量名 - 使用UPPER_SNAKE_CASE
const MAX_SYMPTOMS_COUNT = 20;
const API_BASE_URL = 'https://api.tcm-ai.com';

// 枚举名 - 使用PascalCase
enum HttpStatus {
  OK = 200,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
  INTERNAL_ERROR = 500
}
```

### 2.3 数据库命名规范

#### 2.3.1 表和字段命名
```sql
-- 表名 - 使用snake_case，复数形式
CREATE TABLE patients (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE diagnosis_records (
    id INT PRIMARY KEY AUTO_INCREMENT,
    patient_id INT NOT NULL,
    diagnosis_result JSON,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (patient_id) REFERENCES patients(id)
);

-- 字段名 - 使用snake_case
-- 主键: id
-- 外键: {related_table}_id
-- 时间戳: created_at, updated_at, deleted_at
-- 状态字段: status, is_active, is_deleted
-- 布尔值: is_*, has_*

-- 索引命名 - idx_{table}_{columns}
CREATE INDEX idx_patients_phone ON patients(phone);
CREATE INDEX idx_diagnosis_records_patient_date ON diagnosis_records(patient_id, created_at);

-- 约束命名 - {type}_{table}_{columns}
ALTER TABLE diagnosis_records 
ADD CONSTRAINT fk_diagnosis_records_patient 
FOREIGN KEY (patient_id) REFERENCES patients(id);
```

## 3. 代码结构规范

### 3.1 项目结构规范

#### 3.1.1 后端项目结构
```
TcmAiDiagnosis/
├── src/
│   ├── TcmAiDiagnosis.Web/                    # Web API层
│   │   ├── Controllers/                       # 控制器
│   │   │   ├── PatientsController.cs
│   │   │   ├── DiagnosisController.cs
│   │   │   └── PrescriptionsController.cs
│   │   ├── Middleware/                        # 中间件
│   │   ├── Extensions/                        # 扩展方法
│   │   └── Program.cs
│   ├── TcmAiDiagnosis.Application/            # 应用层
│   │   ├── Services/                          # 应用服务
│   │   │   ├── IPatientService.cs
│   │   │   ├── PatientService.cs
│   │   │   ├── IDiagnosisService.cs
│   │   │   └── DiagnosisService.cs
│   │   ├── Dtos/                            # 数据传输对象
│   │   │   ├── PatientDto.cs
│   │   │   ├── DiagnosisDto.cs
│   │   │   └── PrescriptionDto.cs
│   │   ├── Mappings/                        # 对象映射
│   │   └── Validators/                    # 验证器
│   ├── TcmAiDiagnosis.Domain/              # 领域层
│   │   ├── Entities/                      # 实体
│   │   │   ├── Patient.cs
│   │   │   ├── Diagnosis.cs
│   │   │   └── Prescription.cs
│   │   ├── ValueObjects/                  # 值对象
│   │   ├── Aggregates/                    # 聚合根
│   │   ├── Repositories/                  # 仓储接口
│   │   └── Events/                        # 领域事件
│   ├── TcmAiDiagnosis.Infrastructure/      # 基础设施层
│   │   ├── Persistence/                   # 持久化
│   │   │   ├── Contexts/
│   │   │   ├── Repositories/
│   │   │   └── Configurations/
│   │   ├── ExternalServices/              # 外部服务
│   │   └── Logging/                       # 日志
│   └── TcmAiDiagnosis.Shared/              # 共享库
│       ├── Constants/                      # 常量
│       ├── Exceptions/                     # 异常
│       └── Utilities/                      # 工具类
├── tests/
│   ├── TcmAiDiagnosis.UnitTests/
│   ├── TcmAiDiagnosis.IntegrationTests/
│   └── TcmAiDiagnosis.E2ETests/
└── docs/
    ├── api/
    └── architecture/
```

#### 3.1.2 前端项目结构
```
tcm-ai-frontend/
├── src/
│   ├── components/                    # 组件
│   │   ├── common/                    # 通用组件
│   │   │   ├── Button/
│   │   │   ├── Input/
│   │   │   └── Modal/
│   │   ├── patient/                   # 患者相关组件
│   │   │   ├── PatientCard.vue
│   │   │   ├── PatientForm.vue
│   │   │   └── PatientList.vue
│   │   ├── diagnosis/                 # 诊断相关组件
│   │   │   ├── DiagnosisForm.vue
│   │   │   ├── DiagnosisResult.vue
│   │   │   └── SymptomSelector.vue
│   │   └── prescription/              # 处方相关组件
│   ├── views/                         # 页面视图
│   │   ├── Home/
│   │   ├── Patient/
│   │   ├── Diagnosis/
│   │   └── Prescription/
│   ├── composables/                   # 组合式函数
│   ├── stores/                        # 状态管理
│   │   ├── patient.ts
│   │   ├── diagnosis.ts
│   │   └── auth.ts
│   ├── types/                         # TypeScript类型
│   │   ├── patient.ts
│   │   ├── diagnosis.ts
│   │   └── api.ts
│   ├── services/                      # API服务
│   │   ├── api.ts
│   │   ├── patient.service.ts
│   │   └── diagnosis.service.ts
│   ├── utils/                         # 工具函数
│   ├── assets/                        # 静态资源
│   ├── styles/                        # 样式文件
│   └── App.vue
├── tests/                             # 测试文件
├── docs/                              # 文档
└── package.json
```

### 3.2 文件组织规范

#### 3.2.1 文件命名规则
```yaml
C#文件:
  类文件: {ClassName}.cs
  接口文件: I{InterfaceName}.cs
  配置文件: {Purpose}Configuration.cs
  扩展文件: {Type}Extensions.cs
  
TypeScript文件:
  组件文件: {ComponentName}.vue
  组合式函数: use{FunctionName}.ts
  工具函数: {UtilityName}.utils.ts
  类型定义: {TypeName}.types.ts
  
SQL文件:
  表创建: create_{table_name}.sql
  索引创建: index_{table_name}_{columns}.sql
  数据迁移: migrate_{version}_{description}.sql
  
配置文件:
  环境配置: .env.{environment}
  Docker配置: Dockerfile.{service}
  Kubernetes配置: {service}-{resource}.yaml
```

## 4. 代码风格规范

### 4.1 C#代码风格

#### 4.1.1 代码格式规范
```csharp
// 类定义
public class PatientService : IPatientService
{
    private readonly IPatientRepository _patientRepository;
    private readonly ILogger<PatientService> _logger;
    
    public PatientService(
        IPatientRepository patientRepository,
        ILogger<PatientService> logger)
    {
        _patientRepository = patientRepository ?? throw new ArgumentNullException(nameof(patientRepository));
        _logger = logger ?? throw new ArgumentNullException(nameof(logger));
    }
    
    public async Task<PatientDto> GetPatientByIdAsync(int patientId)
    {
        // 参数验证
        if (patientId <= 0)
        {
            throw new ArgumentException("Patient ID must be greater than 0", nameof(patientId));
        }
        
        try
        {
            _logger.LogInformation("Getting patient with ID: {PatientId}", patientId);
            
            var patient = await _patientRepository.GetByIdAsync(patientId);
            if (patient == null)
            {
                _logger.LogWarning("Patient not found: {PatientId}", patientId);
                return null;
            }
            
            return patient.ToDto();
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error getting patient with ID: {PatientId}", patientId);
            throw;
        }
    }
}

// 方法链式调用
var result = patients
    .Where(p => p.IsActive)
    .OrderBy(p => p.Name)
    .Skip((pageNumber - 1) * pageSize)
    .Take(pageSize)
    .ToList();

// 条件表达式
var status = isActive ? "Active" : "Inactive";

// 字符串插值
var message = $"Patient {patient.Name} (ID: {patient.Id}) was created at {patient.CreatedAt:yyyy-MM-dd}";
```

#### 4.1.2 异常处理规范
```csharp
// 自定义异常
public class PatientNotFoundException : Exception
{
    public int PatientId { get; }
    
    public PatientNotFoundException(int patientId) 
        : base($"Patient with ID {patientId} was not found")
    {
        PatientId = patientId;
    }
}

// 异常处理模式
public async Task<PatientDto> GetPatientAsync(int patientId)
{
    try
    {
        return await _patientRepository.GetByIdAsync(patientId);
    }
    catch (DbException ex)
    {
        _logger.LogError(ex, "Database error while getting patient {PatientId}", patientId);
        throw new ServiceException("Unable to retrieve patient data", ex);
    }
    catch (TimeoutException ex)
    {
        _logger.LogError(ex, "Timeout while getting patient {PatientId}", patientId);
        throw new ServiceException("Request timeout", ex);
    }
}
```

### 4.2 TypeScript代码风格

#### 4.2.1 Vue组件规范
```typescript
<!-- 单文件组件结构 -->
<template>
  <div class="patient-card">
    <h2>{{ patient.name }}</h2>
    <p>Age: {{ patient.age }}</p>
    <button @click="handleEdit" :disabled="isLoading">
      {{ isLoading ? 'Loading...' : 'Edit' }}
    </button>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import type { Patient } from '@/types/patient'
import { usePatientStore } from '@/stores/patient'

// Props定义
interface Props {
  patient: Patient
  showActions?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  showActions: true
})

// Emits定义
const emit = defineEmits<{
  edit: [patient: Patient]
  delete: [patientId: number]
}>()

// 状态管理
const patientStore = usePatientStore()
const isLoading = ref(false)

// 计算属性
const patientAge = computed(() => {
  return new Date().getFullYear() - new Date(props.patient.birthDate).getFullYear()
})

// 方法定义
const handleEdit = async () => {
  try {
    isLoading.value = true
    emit('edit', props.patient)
  } catch (error) {
    console.error('Error editing patient:', error)
  } finally {
    isLoading.value = false
  }
}

// 生命周期钩子
onMounted(() => {
  console.log('Patient card mounted for:', props.patient.name)
})
</script>

<style scoped>
.patient-card {
  padding: 1rem;
  border: 1px solid #ddd;
  border-radius: 8px;
}

.patient-card h2 {
  margin: 0 0 0.5rem 0;
  color: #333;
}
</style>
```

#### 4.2.2 组合式函数规范
```typescript
// usePatient.ts
import { ref, computed, watch } from 'vue'
import type { Patient, PatientQuery } from '@/types/patient'
import { patientService } from '@/services/patient.service'

export function usePatient(patientId?: number) {
  // 状态
  const patient = ref<Patient | null>(null)
  const isLoading = ref(false)
  const error = ref<string | null>(null)
  
  // 计算属性
  const isPatientLoaded = computed(() => patient.value !== null)
  const fullName = computed(() => {
    if (!patient.value) return ''
    return `${patient.value.firstName} ${patient.value.lastName}`
  })
  
  // 方法
  const loadPatient = async (id: number) => {
    isLoading.value = true
    error.value = null
    
    try {
      patient.value = await patientService.getById(id)
    } catch (err) {
      error.value = err instanceof Error ? err.message : 'Unknown error'
      console.error('Failed to load patient:', err)
    } finally {
      isLoading.value = false
    }
  }
  
  const updatePatient = async (updatedPatient: Patient) => {
    isLoading.value = true
    error.value = null
    
    try {
      const result = await patientService.update(updatedPatient)
      patient.value = result
      return result
    } catch (err) {
      error.value = err instanceof Error ? err.message : 'Unknown error'
      throw err
    } finally {
      isLoading.value = false
    }
  }
  
  // 初始化
  if (patientId) {
    loadPatient(patientId)
  }
  
  return {
    patient: readonly(patient),
    isLoading: readonly(isLoading),
    error: readonly(error),
    isPatientLoaded,
    fullName,
    loadPatient,
    updatePatient
  }
}
```

## 5. 注释规范

### 5.1 文档注释

#### 5.1.1 C# XML注释
```csharp
/// <summary>
/// 提供患者相关的业务逻辑处理
/// </summary>
public interface IPatientService
{
    /// <summary>
    /// 根据ID获取患者信息
    /// </summary>
    /// <param name="patientId">患者ID</param>
    /// <returns>患者信息，如果未找到则返回null</returns>
    /// <exception cref="ArgumentException">当patientId小于等于0时抛出</exception>
    Task<PatientDto?> GetPatientByIdAsync(int patientId);
    
    /// <summary>
    /// 创建新患者记录
    /// </summary>
    /// <param name="createPatientDto">患者创建信息</param>
    /// <returns>创建成功的患者信息</returns>
    /// <exception cref="ValidationException">当患者信息验证失败时抛出</exception>
    Task<PatientDto> CreatePatientAsync(CreatePatientDto createPatientDto);
}

/// <summary>
/// 表示一个患者实体
/// </summary>
/// <remarks>
/// 包含患者的基本信息和医疗记录
/// </remarks>
public class Patient
{
    /// <summary>
    /// 患者唯一标识符
    /// </summary>
    public int Id { get; set; }
    
    /// <summary>
    /// 患者姓名
    /// </summary>
    /// <example>张三</example>
    public string Name { get; set; } = string.Empty;
    
    /// <summary>
    /// 出生日期
    /// </summary>
    /// <example>1990-01-15</example>
    public DateTime BirthDate { get; set; }
}
```

#### 5.1.2 TypeScript JSDoc注释
```typescript
/**
 * 患者信息服务
 * 提供患者相关的CRUD操作
 */
export interface PatientService {
  /**
   * 根据ID获取患者信息
   * @param patientId - 患者ID
   * @returns 患者信息，如果未找到则返回null
   * @throws {Error} 当网络错误或服务器错误时抛出
   */
  getPatientById(patientId: number): Promise<Patient | null>
  
  /**
   * 创建新患者记录
   * @param patientData - 患者创建数据
   * @returns 创建成功的患者信息
   * @throws {ValidationError} 当数据验证失败时抛出
   */
  createPatient(patientData: CreatePatientRequest): Promise<Patient>
}

/**
 * 患者实体
 */
export interface Patient {
  /** 患者唯一标识符 */
  id: number
  
  /** 患者姓名 */
  name: string
  
  /** 出生日期 */
  birthDate: string
  
  /** 联系电话 */
  phone?: string
  
  /** 创建时间 */
  createdAt: string
  
  /** 更新时间 */
  updatedAt: string
}
```

### 5.2 行内注释

#### 5.2.1 关键逻辑注释
```csharp
// 计算中医证候得分
// 基于症状权重和持续时间计算综合得分
var syndromeScore = symptoms
    .Where(s => s.IsSelected)
    .Sum(s => s.Weight * s.DurationInDays);

// 如果得分超过阈值，标记为高风险
// 阈值基于历史数据统计得出
if (syndromeScore > HIGH_RISK_THRESHOLD)
{
    diagnosisResult.RiskLevel = RiskLevel.High;
    
    // 记录高风险诊断，用于后续分析
    _logger.LogWarning("High risk diagnosis detected for patient {PatientId}", patientId);
}
```

## 6. 错误处理规范

### 6.1 异常处理原则

#### 6.1.1 异常分类
```csharp
// 业务异常
public class BusinessException : Exception
{
    public string ErrorCode { get; }
    
    public BusinessException(string errorCode, string message) 
        : base(message)
    {
        ErrorCode = errorCode;
    }
}

// 验证异常
public class ValidationException : BusinessException
{
    public List<ValidationError> Errors { get; }
    
    public ValidationException(List<ValidationError> errors) 
        : base("VALIDATION_ERROR", "Validation failed")
    {
        Errors = errors;
    }
}

// 资源未找到异常
public class NotFoundException : BusinessException
{
    public NotFoundException(string resourceType, object resourceId) 
        : base("NOT_FOUND", $"{resourceType} with ID {resourceId} was not found")
    {
    }
}
```

#### 6.1.2 异常处理模式
```csharp
// 全局异常处理中间件
public class GlobalExceptionHandlingMiddleware
{
    private readonly RequestDelegate _next;
    private readonly ILogger<GlobalExceptionHandlingMiddleware> _logger;
    
    public async Task InvokeAsync(HttpContext context)
    {
        try
        {
            await _next(context);
        }
        catch (Exception ex)
        {
            await HandleExceptionAsync(context, ex);
        }
    }
    
    private async Task HandleExceptionAsync(HttpContext context, Exception exception)
    {
        var response = exception switch
        {
            ValidationException ex => new ErrorResponse
            {
                StatusCode = 400,
                ErrorCode = ex.ErrorCode,
                Message = ex.Message,
                Details = ex.Errors
            },
            NotFoundException ex => new ErrorResponse
            {
                StatusCode = 404,
                ErrorCode = ex.ErrorCode,
                Message = ex.Message
            },
            BusinessException ex => new ErrorResponse
            {
                StatusCode = 400,
                ErrorCode = ex.ErrorCode,
                Message = ex.Message
            },
            _ => new ErrorResponse
            {
                StatusCode = 500,
                ErrorCode = "INTERNAL_ERROR",
                Message = "An unexpected error occurred"
            }
        };
        
        _logger.LogError(exception, "Unhandled exception occurred");
        
        context.Response.StatusCode = response.StatusCode;
        await context.Response.WriteAsJsonAsync(response);
    }
}
```

### 6.2 错误返回格式

#### 6.2.1 统一错误响应
```csharp
public class ErrorResponse
{
    public int StatusCode { get; set; }
    public string ErrorCode { get; set; } = string.Empty;
    public string Message { get; set; } = string.Empty;
    public object? Details { get; set; }
    public DateTime Timestamp { get; set; } = DateTime.UtcNow;
    public string Path { get; set; } = string.Empty;
}

// 使用示例
[ApiController]
[Route("api/[controller]")]
public class PatientsController : ControllerBase
{
    [HttpGet("{patientId}")]
    public async Task<ActionResult<PatientDto>> GetPatient(int patientId)
    {
        try
        {
            var patient = await _patientService.GetPatientByIdAsync(patientId);
            if (patient == null)
            {
                return NotFound(new ErrorResponse
                {
                    StatusCode = 404,
                    ErrorCode = "PATIENT_NOT_FOUND",
                    Message = $"Patient with ID {patientId} was not found",
                    Path = $"/api/patients/{patientId}"
                });
            }
            
            return Ok(patient);
        }
        catch (ValidationException ex)
        {
            return BadRequest(new ErrorResponse
            {
                StatusCode = 400,
                ErrorCode = "VALIDATION_ERROR",
                Message = ex.Message,
                Details = ex.Errors,
                Path = $"/api/patients/{patientId}"
            });
        }
    }
}
```

## 7. 日志规范

### 7.1 日志级别使用

#### 7.1.1 日志级别定义
```csharp
public static class LogLevelDefinitions
{
    /// <summary>
    /// 用于记录详细的调试信息，仅在开发环境中启用
    /// </summary>
    public const LogLevel Debug = LogLevel.Debug;
    
    /// <summary>
    /// 用于记录一般信息性消息
    /// </summary>
    public const LogLevel Information = LogLevel.Information;
    
    /// <summary>
    /// 用于记录警告信息，表示可能出现问题
    /// </summary>
    public const LogLevel Warning = LogLevel.Warning;
    
    /// <summary>
    /// 用于记录错误信息，表示功能失败但系统仍可继续运行
    /// </summary>
    public const LogLevel Error = LogLevel.Error;
    
    /// <summary>
    /// 用于记录严重错误信息，表示系统可能无法继续运行
    /// </summary>
    public const LogLevel Critical = LogLevel.Critical;
}
```

#### 7.1.2 日志记录示例
```csharp
public class PatientService : IPatientService
{
    private readonly ILogger<PatientService> _logger;
    
    public async Task<PatientDto> CreatePatientAsync(CreatePatientDto dto)
    {
        _logger.LogInformation("Creating new patient with name: {PatientName}", dto.Name);
        
        try
        {
            var validationResult = await _validator.ValidateAsync(dto);
            if (!validationResult.IsValid)
            {
                _logger.LogWarning("Validation failed for patient creation: {Errors}", 
                    string.Join(", ", validationResult.Errors.Select(e => e.ErrorMessage)));
                throw new ValidationException(validationResult.Errors);
            }
            
            var patient = dto.ToEntity();
            _logger.LogDebug("Patient entity created: {@Patient}", patient);
            
            var createdPatient = await _patientRepository.AddAsync(patient);
            _logger.LogInformation("Patient created successfully with ID: {PatientId}", createdPatient.Id);
            
            return createdPatient.ToDto();
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error creating patient with name: {PatientName}", dto.Name);
            throw;
        }
    }
}
```

### 7.2 结构化日志

#### 7.2.1 日志模板规范
```csharp
// 使用结构化日志模板
public static class LogTemplates
{
    public const string PatientCreated = "Patient created with ID: {PatientId}, Name: {PatientName}, Age: {PatientAge}";
    public const string DiagnosisCompleted = "Diagnosis completed for patient {PatientId}, Result: {DiagnosisResult}, Score: {ConfidenceScore}";
    public const string PrescriptionGenerated = "Prescription generated for diagnosis {DiagnosisId}, Total medicines: {MedicineCount}, Total cost: {TotalCost}";
}

// 使用示例
_logger.LogInformation(LogTemplates.PatientCreated, 
    patient.Id, patient.Name, patient.Age);
```

## 8. 测试规范

### 8.1 单元测试规范

#### 8.1.1 测试命名规范
```csharp
// 测试类命名: {ClassName}Tests
public class PatientServiceTests
{
    // 测试方法命名: {MethodName}_{Scenario}_{ExpectedResult}
    [Fact]
    public void GetPatientById_ValidId_ReturnsPatient()
    {
        // Arrange
        var patientId = 1;
        var expectedPatient = new Patient { Id = patientId, Name = "Test Patient" };
        
        // Act
        var result = _patientService.GetPatientByIdAsync(patientId);
        
        // Assert
        Assert.NotNull(result);
        Assert.Equal(expectedPatient.Id, result.Id);
        Assert.Equal(expectedPatient.Name, result.Name);
    }
    
    [Fact]
    public void GetPatientById_InvalidId_ThrowsArgumentException()
    {
        // Arrange
        var invalidId = -1;
        
        // Act & Assert
        Assert.ThrowsAsync<ArgumentException>(
            () => _patientService.GetPatientByIdAsync(invalidId));
    }
    
    [Fact]
    public async Task CreatePatientAsync_ValidData_ReturnsCreatedPatient()
    {
        // Arrange
        var createDto = new CreatePatientDto
        {
            Name = "New Patient",
            BirthDate = new DateTime(1990, 1, 1),
            Phone = "13800138000"
        };
        
        // Act
        var result = await _patientService.CreatePatientAsync(createDto);
        
        // Assert
        Assert.NotNull(result);
        Assert.Equal(createDto.Name, result.Name);
        Assert.True(result.Id > 0);
    }
}
```

### 8.2 测试数据规范

#### 8.2.1 测试数据构建器
```csharp
// 测试数据构建器模式
public class PatientBuilder
{
    private int _id = 1;
    private string _name = "Test Patient";
    private DateTime _birthDate = new DateTime(1990, 1, 1);
    private string _phone = "13800138000";
    
    public PatientBuilder WithId(int id)
    {
        _id = id;
        return this;
    }
    
    public PatientBuilder WithName(string name)
    {
        _name = name;
        return this;
    }
    
    public PatientBuilder WithBirthDate(DateTime birthDate)
    {
        _birthDate = birthDate;
        return this;
    }
    
    public Patient Build()
    {
        return new Patient
        {
            Id = _id,
            Name = _name,
            BirthDate = _birthDate,
            Phone = _phone
        };
    }
}

// 使用示例
var patient = new PatientBuilder()
    .WithId(123)
    .WithName("John Doe")
    .WithBirthDate(new DateTime(1985, 5, 15))
    .Build();
```

## 9. 代码审查规范

### 9.1 审查清单

#### 9.1.1 代码审查检查表
```yaml
功能正确性:
  - [ ] 代码实现了需求规格说明的功能
  - [ ] 边界条件处理正确
  - [ ] 异常处理完整
  - [ ] 并发安全性考虑

代码质量:
  - [ ] 命名规范符合标准
  - [ ] 代码结构清晰
  - [ ] 注释完整准确
  - [ ] 重复代码已消除

性能优化:
  - [ ] 数据库查询优化
  - [ ] 内存使用合理
  - [ ] 算法复杂度可接受
  - [ ] 缓存使用适当

安全性:
  - [ ] 输入验证完整
  - [ ] SQL注入防护
  - [ ] 敏感信息保护
  - [ ] 权限控制正确

测试:
  - [ ] 单元测试覆盖
  - [ ] 集成测试通过
  - [ ] 边界条件测试
  - [ ] 异常场景测试
```

### 9.2 审查流程

#### 9.2.1 Pull Request模板
```markdown
## Pull Request描述

### 变更内容
- 新增患者管理功能
- 优化诊断算法性能
- 修复处方生成bug

### 测试
- [ ] 单元测试已通过
- [ ] 集成测试已通过
- [ ] 手动测试已完成

### 代码审查清单
- [ ] 代码符合编码规范
- [ ] 注释完整准确
- [ ] 异常处理完整
- [ ] 性能考虑充分

### 相关Issue
Closes #123
Relates to #456
```

---

**文档版本：** V1.0  
**创建日期：** 2024-02-20  
**最后更新：** 2024-02-20  
**编制：** 开发团队  
**审核：** 技术负责人  
**批准：** 技术总监