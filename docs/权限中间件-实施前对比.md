# 权限验证中间件 - 实施前后对比文档

## 文档说明

本文档详细列出了实施权限验证中间件系统前后的代码变更，帮助你了解将要进行的修改。

---

## 📋 目录结构变更

### 新增目录

```
TcmAiDiagnosis.Web/
├── Attributes/          [新增] 权限特性目录
├── Middleware/          [新增] 中间件目录
└── Configuration/       [新增] 配置类目录

TcmAiDiagnosis.Domain/
└── Services/
    ├── IPermissionService.cs              [新增]
    ├── PermissionService.cs               [新增]
    ├── IPermissionCacheService.cs         [新增]
    ├── PermissionCacheService.cs          [新增]
    ├── IPermissionAuditLogger.cs          [新增]
    └── PermissionAuditLogger.cs           [新增]
```

---

## 📄 文件级别变更清单

### 1. 新增文件（共13个）

#### Domain层（6个文件）

| 文件路径 | 说明 | 行数估算 |
|---------|------|---------|
| `TcmAiDiagnosis.Domain/Services/IPermissionService.cs` | 权限服务接口 | ~50行 |
| `TcmAiDiagnosis.Domain/Services/PermissionService.cs` | 权限服务实现 | ~200行 |
| `TcmAiDiagnosis.Domain/Services/IPermissionCacheService.cs` | 缓存服务接口 | ~20行 |
| `TcmAiDiagnosis.Domain/Services/PermissionCacheService.cs` | 缓存服务实现 | ~100行 |
| `TcmAiDiagnosis.Domain/Services/IPermissionAuditLogger.cs` | 审计日志接口 | ~20行 |
| `TcmAiDiagnosis.Domain/Services/PermissionAuditLogger.cs` | 审计日志实现 | ~80行 |

#### Web层（4个文件）

| 文件路径 | 说明 | 行数估算 |
|---------|------|---------|
| `TcmAiDiagnosis.Web/Attributes/RequirePermissionAttribute.cs` | 权限特性 | ~40行 |
| `TcmAiDiagnosis.Web/Middleware/PermissionAuthorizationMiddleware.cs` | 权限中间件 | ~150行 |
| `TcmAiDiagnosis.Web/Configuration/PermissionSettings.cs` | 配置类 | ~30行 |
| `TcmAiDiagnosis.Web/Extensions/PermissionServiceExtensions.cs` | 服务注册扩展 | ~40行 |

#### 枚举（1个文件）

| 文件路径 | 说明 | 行数估算 |
|---------|------|---------|
| `TcmAiDiagnosis.Entities/Enums/PermissionCheckMode.cs` | 权限检查模式枚举 | ~15行 |

#### 测试文件（可选，2个文件）

| 文件路径 | 说明 | 行数估算 |
|---------|------|---------|
| `TcmAiDiagnosis.Tests/Services/PermissionServiceTests.cs` | 权限服务单元测试 | ~300行 |
| `TcmAiDiagnosis.Tests/Middleware/PermissionMiddlewareTests.cs` | 中间件集成测试 | ~200行 |

### 2. 修改文件（3个）

| 文件路径 | 修改内容 | 影响范围 |
|---------|---------|---------|
| `TcmAiDiagnosis.Web/Program.cs` | 注册权限服务和中间件 | +15行 |
| `TcmAiDiagnosis.Web/appsettings.json` | 添加权限配置节 | +10行 |
| `TcmAiDiagnosis.Web/Controller/*.cs` | 添加权限特性标注 | 每个控制器+1-5行 |

---

## 🔍 详细代码对比

### 1. Program.cs 变更

#### 当前代码（简化版）

```csharp
public class Program
{
    public static void Main(string[] args)
    {
        var builder = WebApplication.CreateBuilder(args);
        
        // ... 现有服务注册 ...
        builder.Services.AddScoped<UserDomain>();
        builder.Services.AddScoped<TenantDomain>();
        // ... 其他Domain服务 ...
        
        var app = builder.Build();
        
        // ... 中间件配置 ...
        app.UseStaticFiles();
        app.UseRouting();
        app.UseAuthentication();
        app.UseAuthorization();
        
        app.MapRazorPages();
        app.MapControllers();
        
        app.Run();
    }
}
```

#### 修改后代码

```csharp
public class Program
{
    public static void Main(string[] args)
    {
        var builder = WebApplication.CreateBuilder(args);
        
        // ... 现有服务注册 ...
        builder.Services.AddScoped<UserDomain>();
        builder.Services.AddScoped<TenantDomain>();
        // ... 其他Domain服务 ...
        
        // ✨ 新增：注册权限系统服务
        builder.Services.AddPermissionServices(builder.Configuration);
        
        var app = builder.Build();
        
        // ... 中间件配置 ...
        app.UseStaticFiles();
        app.UseRouting();
        app.UseAuthentication();
        app.UseAuthorization();
        
        // ✨ 新增：注册权限验证中间件
        app.UsePermissionAuthorization();
        
        app.MapRazorPages();
        app.MapControllers();
        
        app.Run();
    }
}
```

**变更说明**：
- ✅ 添加 `AddPermissionServices()` 扩展方法调用
- ✅ 添加 `UsePermissionAuthorization()` 中间件
- ⚠️ 中间件顺序：必须在 `UseAuthorization()` 之后

---

### 2. appsettings.json 变更

#### 当前配置（简化版）

```json
{
  "ConnectionStrings": {
    "Connection": "server=localhost;database=tcm-ai-diagnosis;..."
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  }
}
```

#### 修改后配置

```json
{
  "ConnectionStrings": {
    "Connection": "server=localhost;database=tcm-ai-diagnosis;..."
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  },
  
  // ✨ 新增：权限系统配置
  "PermissionSettings": {
    "EnablePermissionCheck": true,
    "CacheExpirationMinutes": 30,
    "WhitelistPaths": [
      "/api/auth/login",
      "/api/health",
      "/swagger",
      "/Identity/Account/Login",
      "/Identity/Account/Logout"
    ],
    "AuditLogLevel": "Warning",
    "EnableDetailedErrors": false
  }
}
```

**变更说明**：
- ✅ 添加 `PermissionSettings` 配置节
- ✅ 可配置是否启用权限检查
- ✅ 可配置缓存过期时间
- ✅ 可配置白名单路径

---

### 3. 控制器变更示例

#### 当前代码（HerbController.cs）

```csharp
[ApiController]
[Route("api/[controller]")]
public class HerbController : ControllerBase
{
    private readonly HerbDomain _herbDomain;
    
    public HerbController(HerbDomain herbDomain)
    {
        _herbDomain = herbDomain;
    }
    
    [HttpGet]
    public async Task<IActionResult> GetHerbs()
    {
        var herbs = await _herbDomain.GetAllHerbsAsync();
        return Ok(herbs);
    }
    
    [HttpPost]
    public async Task<IActionResult> CreateHerb([FromBody] HerbDto dto)
    {
        var result = await _herbDomain.CreateHerbAsync(dto);
        return Ok(result);
    }
}
```

#### 修改后代码

```csharp
[ApiController]
[Route("api/[controller]")]
public class HerbController : ControllerBase
{
    private readonly HerbDomain _herbDomain;
    
    public HerbController(HerbDomain herbDomain)
    {
        _herbDomain = herbDomain;
    }
    
    // ✨ 新增：添加权限特性
    [HttpGet]
    [RequirePermission(PermissionConstants.KnowledgeBase.ViewHerb)]
    public async Task<IActionResult> GetHerbs()
    {
        var herbs = await _herbDomain.GetAllHerbsAsync();
        return Ok(herbs);
    }
    
    // ✨ 新增：添加权限特性
    [HttpPost]
    [RequirePermission(PermissionConstants.KnowledgeBase.ViewHerb)]
    public async Task<IActionResult> CreateHerb([FromBody] HerbDto dto)
    {
        var result = await _herbDomain.CreateHerbAsync(dto);
        return Ok(result);
    }
}
```

**变更说明**：
- ✅ 每个需要权限控制的方法添加 `[RequirePermission]` 特性
- ✅ 使用 `PermissionConstants` 中定义的权限代码
- ✅ 支持多个权限：`[RequirePermission("perm1", "perm2")]`
- ✅ 支持权限模式：`[RequirePermission("perm1", "perm2", Mode = PermissionCheckMode.RequireAny)]`

---

## 🆕 新增核心组件详解

### 1. RequirePermissionAttribute（权限特性）

**文件位置**：`TcmAiDiagnosis.Web/Attributes/RequirePermissionAttribute.cs`

**功能**：用于标注控制器或方法的权限要求

**代码预览**：
```csharp
[AttributeUsage(AttributeTargets.Class | AttributeTargets.Method, AllowMultiple = false)]
public class RequirePermissionAttribute : Attribute
{
    public string[] PermissionCodes { get; }
    public PermissionCheckMode Mode { get; set; } = PermissionCheckMode.RequireAll;
    public bool AllowTemporary { get; set; } = true;
    
    public RequirePermissionAttribute(params string[] permissionCodes)
    {
        PermissionCodes = permissionCodes ?? throw new ArgumentNullException(nameof(permissionCodes));
    }
}
```

**使用示例**：
```csharp
// 单个权限
[RequirePermission(PermissionConstants.PatientManagement.Create)]

// 多个权限（需要全部）
[RequirePermission(
    PermissionConstants.PatientManagement.Edit,
    PermissionConstants.PatientManagement.View
)]

// 多个权限（需要任一）
[RequirePermission(
    PermissionConstants.PatientManagement.View,
    PermissionConstants.PatientManagement.Search,
    Mode = PermissionCheckMode.RequireAny
)]
```

---

### 2. PermissionAuthorizationMiddleware（权限中间件）

**文件位置**：`TcmAiDiagnosis.Web/Middleware/PermissionAuthorizationMiddleware.cs`

**功能**：拦截HTTP请求并验证权限

**核心流程**：
```
1. 检查是否在白名单中 → 是 → 放行
2. 提取端点权限要求 → 无要求 → 放行
3. 检查用户是否已认证 → 否 → 返回401
4. 获取用户ID和租户ID
5. 调用PermissionService验证权限
6. 有权限 → 放行
7. 无权限 → 记录审计日志 → 返回403
```

**代码预览**：
```csharp
public class PermissionAuthorizationMiddleware
{
    private readonly RequestDelegate _next;
    private readonly ILogger<PermissionAuthorizationMiddleware> _logger;
    
    public async Task InvokeAsync(
        HttpContext context,
        IPermissionService permissionService,
        IPermissionAuditLogger auditLogger)
    {
        // 1. 白名单检查
        if (IsWhitelisted(context.Request.Path))
        {
            await _next(context);
            return;
        }
        
        // 2. 提取权限要求
        var endpoint = context.GetEndpoint();
        var permissionAttribute = endpoint?.Metadata
            .GetMetadata<RequirePermissionAttribute>();
        
        if (permissionAttribute == null)
        {
            await _next(context);
            return;
        }
        
        // 3. 认证检查
        if (!context.User.Identity?.IsAuthenticated ?? true)
        {
            context.Response.StatusCode = 401;
            await context.Response.WriteAsJsonAsync(new 
            { 
                error = "Unauthorized",
                message = "用户未认证" 
            });
            return;
        }
        
        // 4. 权限验证
        var userId = GetUserId(context.User);
        var tenantId = GetTenantId(context.User);
        
        var hasPermission = await permissionService.CheckPermissionsAsync(
            userId,
            tenantId,
            permissionAttribute.PermissionCodes,
            permissionAttribute.Mode,
            permissionAttribute.AllowTemporary
        );
        
        if (!hasPermission)
        {
            // 记录审计日志
            await auditLogger.LogPermissionDeniedAsync(
                userId, tenantId, context.Request.Path,
                permissionAttribute.PermissionCodes,
                context.Connection.RemoteIpAddress?.ToString()
            );
            
            context.Response.StatusCode = 403;
            await context.Response.WriteAsJsonAsync(new 
            { 
                error = "Forbidden",
                message = "权限不足",
                requiredPermissions = permissionAttribute.PermissionCodes
            });
            return;
        }
        
        await _next(context);
    }
}
```

---

### 3. IPermissionService（权限服务接口）

**文件位置**：`TcmAiDiagnosis.Domain/Services/IPermissionService.cs`

**功能**：定义权限服务接口

**代码预览**：
```csharp
public interface IPermissionService
{
    /// <summary>
    /// 检查用户是否具有指定权限
    /// </summary>
    Task<bool> CheckPermissionAsync(
        int userId, 
        int? tenantId, 
        string permissionCode,
        bool allowTemporary = true);
    
    /// <summary>
    /// 检查用户是否具有多个权限
    /// </summary>
    Task<bool> CheckPermissionsAsync(
        int userId,
        int? tenantId,
        string[] permissionCodes,
        PermissionCheckMode mode = PermissionCheckMode.RequireAll,
        bool allowTemporary = true);
    
    /// <summary>
    /// 获取用户的所有权限
    /// </summary>
    Task<List<string>> GetUserPermissionsAsync(
        int userId,
        int? tenantId,
        bool includeTemporary = true);
    
    /// <summary>
    /// 获取角色的所有权限
    /// </summary>
    Task<List<string>> GetRolePermissionsAsync(int roleId);
    
    /// <summary>
    /// 刷新用户权限缓存
    /// </summary>
    Task RefreshUserPermissionCacheAsync(int userId, int? tenantId);
    
    /// <summary>
    /// 刷新角色权限缓存
    /// </summary>
    Task RefreshRolePermissionCacheAsync(int roleId);
}
```

---

### 4. PermissionService（权限服务实现）

**文件位置**：`TcmAiDiagnosis.Domain/Services/PermissionService.cs`

**功能**：实现权限验证逻辑，包含缓存和数据库查询

**核心逻辑**：
```csharp
public class PermissionService : IPermissionService
{
    private readonly TcmAiDiagnosisContext _context;
    private readonly IPermissionCacheService _cacheService;
    private readonly ILogger<PermissionService> _logger;
    
    public async Task<List<string>> GetUserPermissionsAsync(
        int userId,
        int? tenantId,
        bool includeTemporary)
    {
        // 1. 尝试从缓存获取
        var cacheKey = $"user_permissions:{userId}:{tenantId}";
        var cached = await _cacheService.GetAsync<List<string>>(cacheKey);
        if (cached != null)
        {
            return cached;
        }
        
        // 2. 从数据库查询
        var permissions = new List<string>();
        
        // 2.1 获取用户的角色权限
        var rolePermissions = await _context.Users
            .Where(u => u.Id == userId)
            .SelectMany(u => u.Roles)
            .SelectMany(r => r.RolePermissions)
            .Where(rp => rp.IsGranted && rp.Permission.IsActive)
            .Select(rp => rp.Permission.PermissionCode)
            .Distinct()
            .ToListAsync();
        
        permissions.AddRange(rolePermissions);
        
        // 2.2 获取临时权限（如果允许）
        if (includeTemporary)
        {
            var now = DateTime.UtcNow;
            var tempPermissions = await _context.TemporaryPermissions
                .Where(tp => 
                    tp.UserId == userId &&
                    tp.TenantId == tenantId &&
                    tp.Status == "Active" &&
                    tp.ValidFrom <= now &&
                    tp.ValidTo >= now)
                .Join(_context.Permissions,
                    tp => tp.PermissionId,
                    p => p.PermissionId,
                    (tp, p) => p.PermissionCode)
                .ToListAsync();
            
            permissions.AddRange(tempPermissions);
        }
        
        // 3. 缓存结果
        await _cacheService.SetAsync(
            cacheKey, 
            permissions, 
            TimeSpan.FromMinutes(30));
        
        return permissions;
    }
}
```

---

### 5. PermissionCacheService（缓存服务）

**文件位置**：`TcmAiDiagnosis.Domain/Services/PermissionCacheService.cs`

**功能**：提供权限数据缓存功能

**代码预览**：
```csharp
public class PermissionCacheService : IPermissionCacheService
{
    private readonly IMemoryCache _memoryCache;
    private readonly ILogger<PermissionCacheService> _logger;
    
    public async Task<T?> GetAsync<T>(string key)
    {
        return _memoryCache.TryGetValue(key, out T? value) ? value : default;
    }
    
    public async Task SetAsync<T>(string key, T value, TimeSpan? expiration = null)
    {
        var options = new MemoryCacheEntryOptions
        {
            AbsoluteExpirationRelativeToNow = expiration ?? TimeSpan.FromMinutes(30)
        };
        
        _memoryCache.Set(key, value, options);
    }
    
    public async Task RemoveAsync(string key)
    {
        _memoryCache.Remove(key);
    }
}
```

---

### 6. PermissionAuditLogger（审计日志）

**文件位置**：`TcmAiDiagnosis.Domain/Services/PermissionAuditLogger.cs`

**功能**：记录权限验证失败和临时权限使用

**代码预览**：
```csharp
public class PermissionAuditLogger : IPermissionAuditLogger
{
    private readonly TcmAiDiagnosisContext _context;
    private readonly ILogger<PermissionAuditLogger> _logger;
    
    public async Task LogPermissionDeniedAsync(
        int userId,
        int? tenantId,
        string requestPath,
        string[] requiredPermissions,
        string? ipAddress)
    {
        var log = new PermissionChangeLog
        {
            UserId = userId,
            TenantId = tenantId,
            ChangeType = "PermissionDenied",
            ChangeDetails = $"访问被拒绝: {requestPath}",
            RequiredPermissions = string.Join(", ", requiredPermissions),
            IpAddress = ipAddress,
            CreatedAt = DateTime.UtcNow
        };
        
        _context.PermissionChangeLogs.Add(log);
        await _context.SaveChangesAsync();
        
        _logger.LogWarning(
            "权限验证失败 - 用户ID: {UserId}, 租户ID: {TenantId}, 路径: {Path}, 所需权限: {Permissions}",
            userId, tenantId, requestPath, string.Join(", ", requiredPermissions));
    }
}
```

---

## 📊 数据库变更

### 无需新增表

✅ 权限系统使用现有的数据库表：
- `permissions` - 权限定义表（已存在）
- `role_permissions` - 角色权限关联表（已存在）
- `temporary_permissions` - 临时权限表（已存在）
- `permission_change_logs` - 权限变更日志表（已存在）

### 可能需要的索引优化

```sql
-- 优化权限查询性能
CREATE INDEX idx_role_permissions_lookup 
ON role_permissions(role_id, permission_id, is_granted);

CREATE INDEX idx_permissions_code 
ON permissions(permission_code, is_active);

CREATE INDEX idx_temp_permissions_user_status 
ON temporary_permissions(user_id, status, valid_from, valid_to);
```

---

## 🎯 实施影响评估

### 对现有功能的影响

| 影响项 | 影响程度 | 说明 |
|--------|---------|------|
| 现有API | 🟡 中等 | 需要添加权限特性标注 |
| 用户登录 | 🟢 无影响 | 登录流程不变 |
| 数据库 | 🟢 无影响 | 使用现有表结构 |
| 性能 | 🟡 轻微 | 增加权限检查（<10ms） |
| 部署 | 🟢 无影响 | 无需额外部署步骤 |

### 性能影响

**权限检查性能**：
- 首次查询：~50ms（数据库查询）
- 缓存命中：<1ms（内存读取）
- 缓存过期时间：30分钟（可配置）

**预期性能指标**：
- 99%的请求在10ms内完成权限检查
- 缓存命中率：>95%
- 对整体响应时间影响：<5%

---

## ✅ 实施检查清单

### 阶段1：核心功能（必须）

- [ ] 创建 `PermissionCheckMode` 枚举
- [ ] 创建 `IPermissionService` 接口
- [ ] 创建 `IPermissionCacheService` 接口
- [ ] 创建 `IPermissionAuditLogger` 接口
- [ ] 实现 `RequirePermissionAttribute` 类
- [ ] 实现 `PermissionCacheService` 类
- [ ] 实现 `PermissionService` 类
- [ ] 实现 `PermissionAuditLogger` 类
- [ ] 实现 `PermissionAuthorizationMiddleware` 类
- [ ] 创建 `PermissionSettings` 配置类
- [ ] 在 `Program.cs` 中注册服务
- [ ] 更新 `appsettings.json` 配置

### 阶段2：API集成（必须）

- [ ] 为患者管理API添加权限标注
- [ ] 为诊疗操作API添加权限标注
- [ ] 为治疗方案API添加权限标注
- [ ] 为药材管理API添加权限标注
- [ ] 为库存管理API添加权限标注

### 阶段3：测试和优化（可选）

- [ ] 编写权限服务单元测试
- [ ] 编写中间件集成测试
- [ ] 性能测试和优化
- [ ] 添加数据库索引
- [ ] 编写开发者文档

---

## 🚀 实施建议

### 推荐实施顺序

1. **第1天**：创建基础接口和枚举（任务1）
2. **第2天**：实现权限特性和缓存服务（任务2-3）
3. **第3-4天**：实现权限服务和审计日志（任务4-5）
4. **第5天**：实现权限中间件（任务6）
5. **第6天**：配置和注册服务（任务7）
6. **第7-8天**：为现有API添加权限标注（任务8）
7. **第9-10天**：测试和优化（任务9-11）

### 风险控制

**回滚方案**：
1. 在 `appsettings.json` 中设置 `EnablePermissionCheck: false` 可立即禁用权限检查
2. 移除 `app.UsePermissionAuthorization()` 可完全禁用中间件
3. 所有新增代码都是独立的，不影响现有功能

**测试策略**：
1. 先在开发环境测试
2. 使用白名单确保关键路径可访问
3. 逐步添加权限标注，而不是一次性全部添加
4. 保持详细的日志记录

---

## 📞 需要确认的问题

在开始实施前，请确认以下问题：

### 1. 缓存方案
- [ ] 使用 IMemoryCache（单实例，简单）
- [ ] 使用 IDistributedCache + Redis（多实例，复杂）

**建议**：先使用 IMemoryCache，后续需要时再升级

### 2. 实施范围
- [ ] 只实施核心功能（阶段1）
- [ ] 实施核心功能 + API集成（阶段1+2）
- [ ] 完整实施包括测试（阶段1+2+3）

**建议**：先实施阶段1+2，测试可选

### 3. API优先级
哪些API需要优先添加权限标注？
- [ ] 患者管理API
- [ ] 诊疗操作API
- [ ] 治疗方案API
- [ ] 药材管理API
- [ ] 库存管理API
- [ ] 其他：__________

**建议**：先完成核心业务API（患者、诊疗、治疗）

---

## 📝 总结

### 代码变更统计

- **新增文件**：13个（核心功能）+ 2个（测试，可选）
- **修改文件**：3个（Program.cs, appsettings.json, 控制器）
- **新增代码行数**：约1000行（不含测试）
- **预计工作量**：8-12天

### 核心优势

✅ **无侵入性**：不修改现有业务逻辑  
✅ **可配置**：可随时启用/禁用  
✅ **高性能**：缓存机制，<10ms响应  
✅ **易维护**：清晰的接口和实现分离  
✅ **可扩展**：支持临时权限、多租户等高级场景  

### 下一步

准备好开始实施了吗？

1. **确认上述问题**
2. **开始任务1**：创建基础接口和枚举
3. **或者告诉我**："开始实施权限中间件"

---

**文档版本**：v1.0  
**创建时间**：2026-01-22  
**作者**：Kiro AI Assistant
